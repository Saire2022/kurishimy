/** Number of words shown per slice before transitioning to the next group */
export const WORDS_PER_CHUNK = 35;

/** Default view when opening a lesson */
export const DEFAULT_VIEW_MODE = "dual" as const;
